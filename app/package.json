{
  "name": "ci-cd-test-app",
  "version": "1.0.0",
  "description": "Test app for CI/CD workflows",
  "main": "dist/app.js",
  "scripts": {
    "build": "mkdir -p dist && cp src/app.js dist/app.js",
    "start": "node dist/app.js",
    "dev": "node src/server.js",
    "test": "jest --coverage"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}